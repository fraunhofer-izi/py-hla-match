version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    post_install:
      - pip install poetry
      - poetry install --with dev
    build:
      html:
        - poetry run sphinx-build -b html docs "$READTHEDOCS_OUTPUT/html"